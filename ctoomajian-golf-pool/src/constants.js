// Shared across the app -- not real security (it ships in the built JS,
// same as everything else gated this way), just a deterrent against casual
// tampering. Change this one value to update it everywhere it's used.
export const PASSCODE = "PercivalToots";
